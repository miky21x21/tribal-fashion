// Custom authentication utilities
// Using custom JWT-based authentication system

export async function getCurrentUser() {
  // This function can be implemented to get current user from JWT token
  // For now, returning null as authentication is handled by middleware
  return null;
}